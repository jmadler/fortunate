(function() {
  'use strict';
  // Fortune object
  //   represents an individual fortune
  //   has => fortune

  // FortuneFile object
  //   represents a fortune file
  //   has => fortuneFileId
  //   has => source
  //   has => fortunes (array of Fortune)

  // Fortunate object
  //   the controller
  //   has => fortunes (array of Fortune objects currently unused)
  //   has => fortuneFiles (array of FortuneFile objects available)
  //   shuffles() fortunes on load
  window.addEventListener('load', function() {
    var fortuneElement = document.getElementById('fortune');
    // build Fortunate

    // when fortune is clicked, replace it with a random Fortune  
  });
)();
